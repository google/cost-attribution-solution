# Cost Attribution Solution (Backend)

## Running

1. Create venv
2. Install requirements.txt
3. Define env variables `CONFIG_BUCKET` and `SCOPE`

Run it for testing `CONFIG_BUCKET=rgodoy-cas-configs SCOPE=organizations/57225061579 fastapi dev main.py`

Or `fastapi run main.py`
